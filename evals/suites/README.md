# Eval Suites

This directory contains local connector suites for fixture-level behavior.

The JSON suites are executable by `aoa-xda eval ...`. Markdown `*.suite.md`
files are local suite notes for the `aoa-evals` local-port validator.

`aoa-evals` owns verdict, scoring, regression, and proof doctrine authority.
