# AGENTS.md

`evals/` is the local eval port for `aoa-xda-connector`.

It may contain local suites, intake pressure, and reports. It does not own
central proof doctrine, verdicts, scoring, or regression policy. Those belong
in `aoa-evals`.
