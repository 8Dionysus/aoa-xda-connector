# Local Evals

The local eval port checks connector behavior against tiny no-network fixtures.

It owns local pressure and local suite execution only. It does not promote
central proof.

`aoa-evals` owns verdict, scoring, regression, and proof doctrine authority.
This repo keeps local fixture checks and reports only.
