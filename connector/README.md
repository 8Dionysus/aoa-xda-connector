# Connector Surface

This directory contains XDA source policy, storage policy, profiles, seeds,
fixtures, manifests, and schemas.

It is a public method surface. Heavy generated data belongs in configured
storage roots, not here.
