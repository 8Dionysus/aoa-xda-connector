# AGENTS.md

`.connector-state/` is ignored local connector state.

It may hold tiny generated fixture runs, receipts, indexes, and graph exports
for local smoke tests. It is not source truth and must not be promoted into Git
except for this route card, `README.md`, and `.gitkeep` scaffold files.
