# Repo-Local Connector State

This directory is the default local state root for small no-network fixture
runs.

Generated content under `data/`, `cache/`, and `artifacts/` is ignored. For
larger runs, set `CONNECTOR_DATA_ROOT`, `CONNECTOR_CACHE_ROOT`, and
`CONNECTOR_ARTIFACT_ROOT` to external storage.
